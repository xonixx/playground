name: Test1

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Test1
      run: |
        where sh
        Write-Host "------------"
        Get-Command sh