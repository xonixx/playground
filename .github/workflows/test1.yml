name: Test1

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2

    - name: Test1
      run: |
        where sh
        Write-Host "------------"
        Get-Command sh